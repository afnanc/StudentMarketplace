import Authentication from "../../components/authentication/authentication.component";
// Render the Auth page with the imported Authentication component
const Auth = () => {
  return <Authentication />;
};

export default Auth;
