import UserProfile from "../../components/user-profile/user-profile.component";

// Render the OtherProfile page from the imported UserProfile component
const OtherProfile = () => {
  return <UserProfile />;
};

export default OtherProfile;
