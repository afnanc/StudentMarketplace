import Message from "../../components/messages/message.component";

// Rendering the Message page from the imported Message component
const MessagePage = () => {
  return <Message />;
};

export default MessagePage;
